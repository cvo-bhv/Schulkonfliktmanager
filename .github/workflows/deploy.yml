name: 🚀 Auto-Deploy
on:
  push:
    branches:
      - main  # Ersetze dies durch 'master', falls dein Haupt-Branch so heißt

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Code holen
        uses: actions/checkout@v4

      - name: ⚙️ Node.js installieren
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Abhängigkeiten installieren & bauen
        run: |
          npm install
          npm run build

      - name: 🚀 Per FTP hochladen
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/  # Lädt nur den Inhalt des dist-Ordners hoch
          server-dir: ./      # Hier ggf. den Pfad zu deinem Web-Verzeichnis (z.B. /httpdocs/)
